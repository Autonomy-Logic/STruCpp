{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "cd .. && npm install && npm run build && cd vscode-extension && npm install && npm run bundle",
      "windows": {
        "command": "if not exist \"%APPDATA%\\npm\" mkdir \"%APPDATA%\\npm\" & cd .. & npm install & npm run build & cd vscode-extension & npm install & npm run bundle",
        "options": { "shell": { "executable": "cmd.exe", "args": ["/c"] } }
      },
      "group": "build",
      "label": "npm: bundle",
      "detail": "Install all dependencies, build compiler, and bundle extension",
      "problemMatcher": ["$tsc"]
    }
  ]
}
